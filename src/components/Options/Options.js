import React from 'react';

const Options = ({ option }) => {
    // console.log(option)
    return (
        <div>
            <input type="radio" name="" id="" />
            {option}
        </div>
    );
};

export default Options;